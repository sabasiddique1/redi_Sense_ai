services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: reportiq-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: reportiq
      POSTGRES_USER: reportiq
      POSTGRES_PASSWORD: reportiq
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U reportiq -d reportiq"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  postgres-data:
